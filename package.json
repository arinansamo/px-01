{
  "name": "px-01",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ページに帯を重ねて、余白と寸法と色を測る Chrome 拡張",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "sample": "node sample/serve.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.7",
    "esbuild": "^0.28.2",
    "typescript": "^5.9.3"
  }
}
